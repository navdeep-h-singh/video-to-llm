name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

# Pull requests receive no credentials of any kind. Nothing in this pipeline
# needs one: every test runs against synthetic media and mocked providers.
permissions:
  contents: read

env:
  # Fail loudly rather than silently reaching the network during tests.
  VIDEO_TO_LLM_NO_NETWORK: "1"

jobs:
  test:
    name: ${{ matrix.os }} · py${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
        python: ["3.11", "3.12", "3.13"]

    env:
      # Otherwise uv falls back to `.python-version` and warns on every step.
      UV_PYTHON: ${{ matrix.python }}

    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install FFmpeg (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install FFmpeg (macOS)
        if: runner.os == 'macOS'
        run: brew install ffmpeg

      - name: Install FFmpeg (Windows)
        if: runner.os == 'Windows'
        run: choco install ffmpeg --no-progress -y

      - name: Sync locked dependencies
        run: uv sync --locked --extra dev --python ${{ matrix.python }}

      # `uv run` re-syncs the environment by default, which drops the dev
      # extras the step above just installed and re-picks an interpreter — the
      # first CI run failed nine cells with `Failed to spawn: ruff` while
      # reporting "Using CPython 3.11.9" on a py3.13 job. `--no-sync` makes the
      # deliberate sync above the one that counts.
      - name: Format check
        run: uv run --no-sync ruff format --check .

      - name: Lint
        run: uv run --no-sync ruff check .

      - name: Type check
        run: uv run --no-sync mypy app

      - name: Tests
        run: uv run --no-sync pytest -q --deselect tests/integration/test_live_ollama.py

      - name: Pre-publish audit
        run: uv run --no-sync python scripts/pre_publish_audit.py

      # The README's test-count badge is a claim, and it went stale once. One
      # cell is enough to catch it; running this nine times would only say the
      # same thing nine times.
      - name: README test-count badge is current
        if: runner.os == 'Linux' && matrix.python == '3.12'
        run: uv run --no-sync python scripts/check_test_badge.py

      - name: Synthetic smoke test (no network)
        run: uv run --no-sync video-to-llm smoke-test

  # The source checkout hides packaging bugs: everything resolves relative to the
  # repository, so a file that never made it into the wheel is still found. This
  # job installs the built artifact somewhere else entirely and runs it there.
  # It exists because `migrations_dir()` pointed outside the package and an
  # installed copy could not create its own database — the checkout never noticed.
  packaging:
    name: Installed wheel · ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install FFmpeg (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install FFmpeg (macOS)
        if: runner.os == 'macOS'
        run: brew install ffmpeg

      - name: Install FFmpeg (Windows)
        if: runner.os == 'Windows'
        run: choco install ffmpeg --no-progress -y

      - name: Build the distribution
        run: uv build

      - name: Install the wheel into a clean environment
        run: |
          uv venv /tmp/wheelcheck --python 3.12
          uv pip install --python /tmp/wheelcheck video-to-llm --find-links dist --no-index --offline || uv pip install --python /tmp/wheelcheck dist/*.whl
        shell: bash

      - name: Run the installed command from outside the checkout
        run: |
          cd /tmp
          /tmp/wheelcheck/bin/video-to-llm --output-root /tmp/wheel-root doctor || true
          /tmp/wheelcheck/bin/video-to-llm --output-root /tmp/wheel-root smoke-test
        shell: bash
        if: runner.os != 'Windows'

  # Unverified locally: the machine this was written on has no Docker. The image
  # is built and exercised here or nowhere.
  docker:
    name: Container image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Build
        run: docker build -t video-to-llm:ci .

      - name: The command line works inside the image
        run: |
          docker run --rm video-to-llm:ci --help
          docker run --rm video-to-llm:ci process --help

      - name: End to end on synthetic media, no network
        run: |
          docker run --rm --network none \
            -e VIDEO_TO_LLM_NO_NETWORK=1 \
            video-to-llm:ci smoke-test

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITLEAKS_CONFIG: .gitleaks.toml
